# Big review — Feature/ServiceBuildSeparation

**Plan anchored to commit:** `71ec4f4e1b4d9ee48096f503829744d32bfdfc7e`  _(2026-06-23)_
**Reviewed up to commit:** `72974c9fa5f4c46b87347331982c3b74b1c7ef4e`  _(2026-06-24)_
Net diff reviewed: `db3e86de..71ec4f4e` (5 commits, 151 files, +1455/-949). Move-only files skipped.
Status legend: `[ ]` not yet reviewed · `[x]` reviewed (date) · `[~]` in progress (incomplete — re-review).

## Summary — big review complete (all 4 areas reviewed)

> **All 3 findings resolved 2026-06-24** (BUILD1 fix proven via local `dotnet pack`; BUILD2 + MS1 are config/plan reconciles). See each finding's ✅ resolution note below.

**3 findings across 4 areas: 1 HIGH, 1 MEDIUM, 1 LOW.** The CPM/build-separation mechanics are sound everywhere — per-folder CPM is complete and build-safe (no `NU1009`/`NU1010` in any of the 12 folders), `nuget.config`/`Directory.Build.props` are uniform and carve-safe with the dependency-confusion guard, `IsPackable` opt-in gating is correct, the Phase-0 `.cs` isolation cut (B2B→Payment runtime) is clean and behaviour-preserving, and the `plans/` diff is accurate (the new `B2B.Tenant.Contracts` edge is real and correctly deferred; deleting `PAYMENT_AGNOSTIC_AUDIT.md` is well-founded). Must-fix and watch items:

- **BUILD1 — HIGH — must fix before merge.** `Concertable.Kernel` (the only `IsPackable` project) `ProjectReference`s the non-packable `Concertable.Contracts` with no `PrivateAssets="all"` / DLL-bundling, so the published package declares a feed-absent `Concertable.Contracts` dependency → the Phase 1 `verify-restore` gate fails with **NU1101**. The entire "prove the rails" point of Phase 1 cannot pass as written. Fix: also publish `Concertable.Contracts`, or pick a true leaf project as the first proof package.
- **MS1 — MEDIUM — reconcile the plan.** B2B's E2E projects still `ProjectReference`/`<Using>` `Payment.Seed` (pre-existing, actively used), but the plan's "Evidence carried forward" line (`SERVICE_BUILD_SEPARATION.md:210-211`) claims that leak was "all addressed in Phase 0" while the rewritten Phase 0 correctly scopes the removal to `B2B.Web` only — an internal contradiction. Scope the evidence line to `B2B.Web` and document the full-stack E2E harness as an explicit cross-folder exception (like the AppHost carve-out).
- **BUILD2 — LOW.** `publish-packages.yml`'s push-trigger `paths:` filter lists only 5 folders; as later phases opt packable projects into other folders, a `master` push touching only those won't publish. Extend `paths:` per phase (or broaden to `api/**`).

This branch separates each backend service's **build closure** (Phase 0: cut cross-service *source*
leaks; Phase 1: per-folder CPM + `nuget.config` + MinVer + a GitHub Packages publish workflow). The
load-bearing rule (`plans/SERVICE_BUILD_SEPARATION.md`): **no repo-root version/build config** —
every service folder + the shared-platform folder must carry its own `Directory.Packages.props`,
`Directory.Build.props`, `nuget.config` so a `git subtree split` of that folder restores and builds
standalone. The foundation stage owns the carve-safety + isolation sweep.

## Coverage
- [x] **Foundation: shared build infra, CPM mechanics & publish rails** _(2026-06-23)_ — 49 files — `.github/workflows/publish-packages.yml` `api/Directory.Build.props` `api/Concertable.AppHost.Shared` `api/Concertable.AppHost` `api/Concertable.Auth.Contracts` `api/Concertable.Auth` `api/Concertable.ServiceDefaults` `api/Concertable.Messaging` `api/Concertable.DataAccess` `api/Shared`
- [x] **Phase 0: cross-service source-leak cut (the `.cs` changes)** _(2026-06-24)_ — 16 files — `api/Concertable.B2B/**/*.cs`
- [x] **B2B per-service build files (CPM/props/nuget, no `.cs`)** _(2026-06-24)_ — 34 files — `api/Concertable.B2B ':(exclude)*.cs'`
- [x] **Customer / Payment / Search build files + plans** _(2026-06-24)_ — 52 files — `api/Concertable.Customer` `api/Concertable.Payment` `api/Concertable.Search` `plans`

## Cross-area notes
<!-- one-liners added during a stage for things a LATER stage must verify; struck through when checked. -->
- ~~**→ B2B per-service build files** (raised by Phase 0): Phase 0 cut the Payment-runtime leak from B2B.Web (`Program.cs`) and the integration-test fixtures (csproj now references only `Payment.Client`/`Payment.Contracts`), but B2B's **E2E** test projects still `ProjectReference` and globally `<Using>` `Concertable.Payment.Seed`…~~ **Resolved in B2B-build-files stage → MS1 (MEDIUM).** Verified: the E2E→`Payment.Seed` reference is pre-existing at merge-base `db3e86de`, unchanged by this branch, and actively used (`StripeE2EAccountResolver` in 4 `.cs`). It is the full-stack E2E test-composition layer (also references `Payment.E2ETests.Helpers` + `Search.E2ETests.Helpers`) — defensible as an intended cross-folder exception like AppHosts, but the plan's evidence (`SERVICE_BUILD_SEPARATION.md:209-211`) wrongly claims it was "all addressed in Phase 0." See MS1 for the reconcile.

## Findings
<!-- appended per area; finding IDs continue across areas: MS#, MB#, BUG#, SEED#, CV#, BUILD# -->

## Foundation: shared build infra, CPM mechanics & publish rails — reviewed 2026-06-23

Reviewed the CI workflow, root-props deletion, the shared package metadata + CPM (`api/Shared/Directory.Build.props`, `Directory.Packages.props`), all per-folder `nuget.config` / `Directory.Build.props` / `Directory.Packages.props`, the opted-in `Concertable.Kernel`, and a representative set of `.csproj` version-strips (Auth, ServiceDefaults, DataAccess.Infrastructure, Messaging, AppHost.Shared). The CPM mechanics are sound: versions cleanly centralized, `IsPackable` opt-in gating is correct (Directory.Build.props sets the `false` default, Kernel overrides to `true`), `nuget.config` files are uniform with a correct dependency-confusion `packageSourceMapping`, Meziantou moved to a `GlobalPackageReference` per folder, and the root `api/Directory.Build.props` deletion matches the "no repo-root config / carve-safe folders" rule. Two build/release findings:

- [x] **BUILD1 — HIGH — Build/packaging (microservice carve premise)** — `api/Shared/Concertable.Kernel/Concertable.Kernel.csproj:7,35`, `.github/workflows/publish-packages.yml:74-83`
  **✅ Resolved 2026-06-24** — opted `Concertable.Contracts` into publishing (`IsPackable=true` + `Description`; it already inherits MinVer/metadata from `Shared/Directory.Build.props`). Proven locally: `dotnet pack api/Concertable.slnx` produces `Concertable.Kernel` **and** `Concertable.Contracts` at the same lockstep version (`0.1.0-alpha.0.529`), and Kernel's nuspec declares `<dependency id="Concertable.Contracts" version="0.1.0-alpha.0.529" />` — both push in the same run, so `verify-restore` resolves it (no NU1101). Chosen over `PrivateAssets="all"`, which would ship Kernel without the Contracts types it transitively exposes.

  _Original finding:_ `Concertable.Kernel` is the *only* `IsPackable=true` project, but it `ProjectReference`s `Concertable.Contracts`, which is **not** packable (`api/Shared/Concertable.Contracts/Concertable.Contracts.csproj` has no `IsPackable`, inherits the folder default `false`) and is never packed/pushed. `dotnet pack` turns `ProjectReference`s into nuspec `<dependency>` entries by default — there is no `PrivateAssets="all"` on the reference and no DLL-bundling target (the only `Directory.Build.targets` are in test folders) — so the published `Concertable.Kernel` package declares a dependency on `Concertable.Contracts <ver>` that is absent from the feed. The Phase 1 `verify-restore` job (`dotnet add package Concertable.Kernel --prerelease; dotnet restore`) will then fail with **NU1101 (Concertable.Contracts not found)** — the "prove the rails" gate that is the entire point of Phase 1 cannot pass as written. (If instead pack omits the non-packable reference, restore passes but the Kernel package ships *without* the Contracts types it transitively exposes — still broken for a real consumer.) Fix: opt `Concertable.Contracts` into publishing too (it's already in the plan's publishable set and already under the workflow's `api/Shared/**` path filter), or pick a true leaf project (no `ProjectReference`s) as Phase 1's first proof package.

- [x] **BUILD2 — LOW — CI** — `.github/workflows/publish-packages.yml:10-16`
  **✅ Resolved 2026-06-24** — dropped the hand-maintained 5-folder list for the coarse backend boundary `paths: ['api/**']`. Investigated the idiomatic enterprise pattern first: real microservice shops never sync a per-package path list — they gate publishing on `IsPackable` (+ tag/change triggers), which is the single source of truth here. `api/**` only avoids re-running on frontend/docs pushes; it can't drift or silently mis-publish, and it's split-clean (each future repo runs the same "pack all `IsPackable`, push" with no path list). Versioning stays lockstep while it's one repo (becomes per-repo independent for free at the split). Comment in the workflow rewritten to document this.

  _Original finding:_ The push trigger's `paths:` filter lists only 5 folders. The pack step packs *every* `IsPackable` project so it "scales without edits," but the **trigger** does not: when a later phase marks a packable project in a folder absent from this list (`B2B.*.Contracts`, `Payment.Contracts`/`Payment.Client`, `Customer.Review.Contracts`), a `master` push touching only that folder won't publish it. Extend `paths:` as each phase opts a folder in (or broaden to `api/**`).

## Phase 0: cross-service source-leak cut (the `.cs` changes) — reviewed 2026-06-24

Reviewed the full net `.cs` diff for `api/Concertable.B2B/` (10 modified, 2 added; pure-delete mock files skipped per the move-only rule). This is the load-bearing isolation cut: B2B's integration-test fixtures previously reached into Payment's **runtime** (`Payment.Domain` `EscrowEntity`, `Payment.Infrastructure.Data.PaymentDbContext`, `Payment.Application.Interfaces[.Webhook]` for the Stripe sub-clients) and B2B.Web registered `Payment.Seed`'s `StripeE2EAccountResolver`. The branch replaces all of it with an in-memory `EscrowStore` + `EscrowRecord` mocked at the `IEscrowClient` boundary, leaving only `Payment.Client` + `Payment.Contracts` (the allowed adapter-boundary projects, since Payment is an *adapter* service a data service may call via gRPC).

Verified, all clean:
- **Isolation (headline lens) holds at the `.cs` level.** Grep of `api/Concertable.B2B/**/*.cs` for `Concertable.Payment.(Domain|Application|Infrastructure|Seed)` returns only one *comment* in an unrelated, unchanged `AssemblyInfo.cs`. No runtime import survives. The fixtures `.csproj` is already consistent (references only `Payment.Client`/`Payment.Contracts`), and `B2B.Web.csproj` carries no Payment-runtime reference.
- **Behaviour faithfully preserved.** `MockEscrowClient.Deposit/Capture` reproduce the old "create payment intent → record a `Held` escrow" path; the happy-path tests (`ApplicationFlatFeeApiTests`/`ApplicationVenueHireApiTests`) still assert `EscrowStatus.Held` + non-empty `ChargeId` against `fixture.Escrows`, now an in-memory `IReadOnlyList<EscrowRecord>` (the `FirstOrDefaultAsync`→`FirstOrDefault` swap correctly follows the type change from `IQueryable` to in-memory). `EscrowStatus` now resolves from `Payment.Client` rather than `Payment.Domain` — consistent across mock, store record, and assertions.
- **`UseFailingPayment` re-route is sound.** It now replaces `IEscrowClient` with `MockEscrowClientFail` (returns `Result.Fail`) instead of the deleted `MockStripeApiClientFail`; the `Accept_ShouldNotCreateDraft_WhenPaymentFails` tests still assert `Assert.Null(draft)`, which the escrow-boundary failure correctly produces (the happy path proves escrow is on the accept→draft path).
- **No danglers.** The 6 deleted Payment-internal mocks (`MockStripeAccountClient[Fail]`, `MockStripeHoldClient[Fail]`, `MockStripePaymentIntentClient`, `MockStripeApiClientFail`) have zero remaining references; the removed `UseDeclineAtVerify()` has no callers; the removed B2B.Web `StripeE2EAccountResolver` singleton was orphaned (all live uses are E2E tests that `new` it / read its static `AccountIds`).
- **DI/state wiring is coherent.** `EscrowStore` is one shared instance registered as both the concrete singleton (injected into `MockEscrowClient`/`OnReset`) and an `IResettable` (cleared between tests); no double-instance hazard.

**No correctness, microservice-isolation, module-boundary, seeding, or C# convention issues found in this area.** One forward item raised for the build-files stage (E2E projects still referencing `Payment.Seed`) — see Cross-area notes.

## B2B per-service build files (CPM/props/nuget) — reviewed 2026-06-24

Reviewed the full net `.csproj` / `props` / `config` diff for `api/Concertable.B2B/` (34 files, no `.cs`). This stage is the CPM conversion of the B2B folder: inline `Version=` stripped from 31 csproj and centralized in a new per-folder `Directory.Packages.props`, plus a new `Directory.Build.props` (`IsPackable` default `false` + `NuGetAudit`/`NoWarn` + Meziantou as a `GlobalPackageReference`) and a new `nuget.config` (`<clear/>` + nuget.org + GitHub Packages, with the `Concertable.*`→github dependency-confusion `packageSourceMapping`).

Verified, all clean:
- **CPM is complete and build-safe.** Every stripped `PackageReference` has a matching `PackageVersion` in `Directory.Packages.props`, and `git grep` over the whole B2B folder confirms **zero** remaining inline `Version=` on any `PackageReference` (so no `NU1009`/`NU1010`). Intra-folder version conflicts are reconciled **upward** exactly as the plan states: `coverlet.collector` 6.0.0→6.0.4 and `Microsoft.NET.Test.Sdk` 17.12.0→17.14.1 (the integration/unit test projects bumped to the E2E projects' higher pins). Both are test-only tooling — low risk.
- **`nuget.config` + `Directory.Build.props` match the Foundation-validated per-folder pattern** (self-contained, carve-safe, dependency-confusion guard, opt-in `IsPackable=false`); nothing new beyond what the Foundation stage already covered.
- **The Phase-0 fixtures-csproj edge is isolation-clean.** `Concertable.B2B.IntegrationTests.Fixtures.csproj` now references `Payment.Client` + `Payment.Contracts` (+ `Stripe.net`) instead of `Payment.Infrastructure` — the allowed adapter-boundary projects (Payment is an *adapter*; a data service may consume its Client/Contracts). Consistent with the `.cs` already reviewed in Phase 0.
- **Harmless housekeeping only:** `Directory.Packages.props` pins `Microsoft.NET.Sdk.Functions` and `Microsoft.NETCore.Targets`, which no B2B csproj references — dead central pins, zero build/runtime effect (CPM silently ignores unused `PackageVersion` entries). Below the bar for a change.

- [x] **MS1 — MEDIUM — Microservice isolation / carve premise (Phase-0 completeness vs. plan claim)** — `api/Concertable.B2B/Tests/E2ETests/Concertable.B2B.E2ETests/Concertable.B2B.E2ETests.csproj:28,47`, `api/Concertable.B2B/Tests/E2ETests/Concertable.B2B.E2ETests.Ui/Concertable.B2B.E2ETests.Ui.csproj:42`
  **✅ Resolved 2026-06-24 (remedy (a))** — reconciled the plan, not the code (the E2E→`Payment.Seed` ref is the intended full-stack harness exception, not a leak). `SERVICE_BUILD_SEPARATION.md`: the old "AppHost note" is now a **Composition-layer note** that explicitly grants the full-stack E2E harness the same cross-folder exception as AppHosts (covering `Payment.Seed` + `Payment/Search.E2ETests.Helpers`); the "Evidence carried forward" line is scoped to `B2B.Web` (the half Phase 0 actually cut) and now points the E2E half at that exception. The internal contradiction is gone.

  _Original finding:_ B2B's E2E projects still `ProjectReference` and globally `<Using>` `Concertable.Payment.Seed` — a non-`*.Contracts`, cross-folder project under `api/Concertable.Payment/`. It is **pre-existing and unchanged** by this branch (present at merge-base `db3e86de`; this stage touched only package versions) and **actively used** — `StripeE2EAccountResolver.AccountIds` in `StripeHooks.cs`, `PaymentSteps.cs`, and `ConcertFinishedTests.cs` assert Stripe test-mode payout destinations. A `git subtree split` of `api/Concertable.B2B/` would orphan the `..\..\..\..\Concertable.Payment\Seed\…` reference. The conflict is with the **plan's own claim**: `plans/SERVICE_BUILD_SEPARATION.md:209-211` lists `B2B.Web/E2ETests → Payment.Seed` as a source leak "all addressed in Phase 0," yet Phase 0 (line 96) removed only the `B2B.Web` half — the **E2ETests** half survives. This is *defensible as an intended exception*: the full-stack E2E harness is the test-composition layer and inherently crosses boundaries (it also references `Payment.E2ETests.Helpers` and even `Search.E2ETests.Helpers`, another data service), analogous to the AppHost carve-out the plan already grants (lines 79-82). But the plan asserts it is **done**, not excepted. Reconcile one of two ways: (a) scope the evidence line to `B2B.Web` only and document the full-stack E2E harness as an explicit cross-folder exception to resolve in Phase 5 / the deployment effort; or (b) finish the cut (harder — the seeded Stripe IDs are Payment-owned). Left as-is, the discrepancy misleads Phase 5 (B2B ref-flip), which the plan implies has nothing left to do here.

## Customer / Payment / Search build files + plans — reviewed 2026-06-24

Reviewed the full net diff for the three remaining service folders (49 `.csproj`/`props`/`config` files) plus the `plans/` diff (`SERVICE_BUILD_SEPARATION.md` +107/-… and the deletion of `PAYMENT_AGNOSTIC_AUDIT.md`, -200). Same CPM conversion pattern as B2B: inline `Version=` stripped and centralized into a new per-folder `Directory.Packages.props`, a new `Directory.Build.props` (`IsPackable` default `false` + `NuGetAudit`/`NoWarn` + Meziantou `GlobalPackageReference`), and a new `nuget.config` (`<clear/>` + nuget.org + GitHub Packages with the `Concertable.*`→github dependency-confusion `packageSourceMapping`).

Verified, all clean:
- **CPM is complete and build-safe in all three folders.** Every version-stripped `PackageReference` has a matching `PackageVersion` (checked exhaustively per folder), and `git grep` confirms **zero** remaining inline `Version=` on any `PackageReference` across Customer/Payment/Search — no `NU1009`/`NU1010`. Intra-folder version reconciliation is correct and *upward*: **Customer** bumps the non-E2E test projects to the E2E projects' higher pins (`coverlet.collector` 6.0.0→6.0.4, `Microsoft.NET.Test.Sdk` 17.12.0→17.14.1, `xunit.runner.visualstudio` 3.0.2→3.1.4 — test tooling only, low risk); **Payment** and **Search** have no Reqnroll/Playwright UI-E2E projects, so no conflict to reconcile and they correctly keep the lower pins (17.12.0/6.0.0/3.0.2). Cross-folder drift (Customer's test SDK ahead of Payment/Search) is expected and fine under per-folder CPM — that is the point of the split.
- **`nuget.config` + `Directory.Build.props` are byte-identical to the Foundation/B2B-validated per-folder pattern** (self-contained, carve-safe, dependency-confusion guard, opt-in `IsPackable=false`). Nothing new beyond what Foundation already covered.
- **Carve-safety / isolation holds.** No `ProjectReference` was added or changed by this diff (it is purely version-strips + 3×{`Directory.Build.props`, `Directory.Packages.props`, `nuget.config`}). The pre-existing cross-folder edges are all either `*.Contracts`/`Payment.Client` (the allowed boundaries) or plan-documented exceptions: **Payment** escapes only to `Auth.Contracts` + the documented `B2B.Tenant.Contracts` edge; **Search** escapes only to `B2B.{Artist,Concert,Venue,Seed}.Contracts` (matches Phase 4); **Customer** escapes to `Auth(.Contracts)`, `Payment.Client/Contracts`, `B2B.{Artist,Concert,User,Venue,Seed}.Contracts` (Phase 6) + the AppHost/`B2B.Seed.Simulator` dev-host ref (Phase 6 explicitly defers it) + the full-stack E2E harness helpers (same exception class as MS1).
- **The plan diff's new claims are accurate.** The "Finding carried forward" callout's new `Payment.Infrastructure → B2B.Tenant.Contracts` edge (the `TenantCreatedEvent` payout-provisioning subscription) is real — confirmed in `Payment.Infrastructure.csproj`, `TenantCreatedHandler.cs`, `ServiceCollectionExtensions.cs`, `Payment.Workers/Program.cs` — it is a `*.Contracts` ref (not a source leak), and it is correctly deferred to Phase 3 (line 167-169). The publishing-model / repo-split note (org-scoped feed survives the split; `publish-packages.yml` does not; cross-repo restore needs the PAT) is sound.
- **Deleting `PAYMENT_AGNOSTIC_AUDIT.md` (-200) is well-founded.** Its work shipped on `master` via the `Feature/payment-proxy` merge: `git grep` confirms Payment's *only* remaining B2B edge is `B2B.Tenant.Contracts` — the dead `B2B.Contract.Contracts` / `B2B.User.Contracts` edges, the `ConcertPayee` projection, and the reverse leak are all gone, so nothing in the audit is silently dropped (its one residual — the *new* `Tenant.Contracts` edge — is carried forward into `SERVICE_BUILD_SEPARATION.md`). Correct plan hygiene per `plans/CLAUDE.md` (a fully-shipped plan is `git rm`'d, not kept as a tombstone).

**No new correctness, microservice-isolation, module-boundary, seeding, C# convention, or build/packaging issues found in this area.** Two prior findings are confirmed still live by this stage:
- **BUILD1 (HIGH) stands.** The plan honestly marks the Phase 1 "prove the rails" gate `⏳ CI run pending` (SERVICE_BUILD_SEPARATION.md:308-312) and only claims local `dotnet pack` succeeded — it does **not** claim the `verify-restore` loop passed, exactly the gap BUILD1 predicts will fail with NU1101 (Kernel packs a dependency on the non-packable `Concertable.Contracts`). No contradiction; this stage reinforces BUILD1.
- **MS1 (MEDIUM) remains open and its doc half is now an internal contradiction.** This same plan-editing commit rewrote Phase 0 to correctly scope the Payment.Seed removal to **`B2B.Web` only** (line 271), yet left the unchanged "Evidence carried forward" line (`SERVICE_BUILD_SEPARATION.md:210-211`) still asserting `B2B.Web/E2ETests → Payment.Seed` was "all addressed in Phase 0." The two now contradict each other in one file. MS1's remedy (a) — scope that evidence line to `B2B.Web` and document the E2E harness as an explicit exception — is the fix and is still outstanding. (Not minted as a new ID: line 210-211 is unchanged by this diff and MS1 already prescribes the exact edit.)

## Incremental review — 2026-06-24 (verifies the fix commit)

Range: `71ec4f4e..72974c9f` (1 commit, `build(cpm): fix Phase 1 publish rails — pack Contracts, simplify trigger, reconcile plan`). 4 files: `publish-packages.yml`, `Concertable.Contracts.csproj`, `SERVICE_BUILD_SEPARATION.md`, and this review doc (the resolution ticks — not product code, not line-reviewed). This run is the verification the prior "✅ Resolved" notes were stamped ahead of — it actually reviews the fix commit through the lenses and re-stamps the watermark from `71ec4f4e` to `72974c9f`.

**All three resolutions verified genuinely sound — no new findings.**

- **BUILD1 (HIGH) — fix is structurally complete, not just plausible.** `Concertable.Contracts.csproj` now carries `<IsPackable>true</IsPackable>` + `<Description>`. Three load-bearing facts confirmed by reading the files (not just trusting the commit message):
  1. **Contracts is a true leaf** — its csproj has **no `ProjectReference`** (only `<FrameworkReference Include="Microsoft.AspNetCore.App" />` and zero `PackageReference`). So packing it cannot declare a feed-absent project dependency — the NU1101 failure mode does **not** recur one level down. (The AspNetCore.App framework reference resolves against the installed shared framework at restore, not the feed; and it predates this commit on Kernel anyway, so it's not introduced here.)
  2. **Kernel's *only* `ProjectReference` is Contracts** (`Concertable.Kernel.csproj:35`) — confirming the workflow comment's claim. This single opt-in closes the entire gap; there is no second non-packable project edge left unaddressed.
  3. **Contracts is in `api/Concertable.slnx`** (line 197, beside Kernel at 199), so `dotnet pack api/Concertable.slnx` genuinely emits `Concertable.Contracts.nupkg`; the push step (`*.nupkg`) ships it in the same run; verify-restore (`add package Concertable.Kernel --prerelease; restore`) then resolves Kernel's lockstep `Concertable.Contracts` dependency from the feed. Both inherit MinVer + metadata from `api/Shared/Directory.Build.props`, so the versions match (`0.1.0-alpha.0.529` per the commit's local `dotnet pack` proof). NU1101 cannot occur as written.
  - Publishing `Concertable.Contracts` is also *architecturally* correct, not just expedient: it's shared intersection code consumed by every service (`api/CLAUDE.md` "Shared code is the intersection"), so a NuGet package is exactly the post-split consumption model.
- **BUILD2 (LOW) — sound.** `paths:` collapsed from the hand-maintained 5-folder list to `api/**` — a strict superset of the old list (nothing that triggered before stops triggering), with `IsPackable` remaining the single source of truth for *what* packs at the pack step. Cannot drift or silently mis-publish; split-clean. The rewritten workflow header comment documents the rationale accurately.
- **MS1 (MEDIUM) — internal contradiction resolved.** The plan is now consistent across all three touch-points: the "AppHost note" became a **Composition-layer note** that explicitly grants the full-stack E2E harness the same cross-folder exception as AppHosts (lines 84-85, naming `Payment.E2ETests.Helpers` / `Search.E2ETests.Helpers` / `Payment.Seed`); Phase 0 (line 104) is scoped to `B2B.Web` only; and the "Evidence carried forward" line (218-220) is now `B2B.Web → Payment.Seed` (Phase 0) **plus** an explicit "the B2B **E2E** projects' `→ Payment.Seed` ref is deliberately *not* cut." `git grep` for `Payment.Seed` / `Phase 0` surfaces no remaining line that still claims the E2E half was cut. The discrepancy that misled Phase 5 is gone.

**Outcome: all 3 big-review findings closed and verified. No correctness, microservice-isolation, module-boundary, seeding, C# convention, or build/packaging issues in the fix commit.** This branch's review is fully current at `72974c9f`.
